# AtEnd 游戏设计与技术规格

> 状态：前期设计基线  
> 引擎：Godot 4  
> 语言：C#  
> 目标平台：PC  
> 文档用途：作为原型开发、谱面格式和后续讨论的共同依据。

## 1. 项目定位

AtEnd 是一款使用键盘游玩的硬核精确判定音游。轨道采用类似《CHUNITHM》的斜向透视表现，但不包含 3D 舞台演出。

游戏逻辑与渲染采用 2D；通过梯形轨道、坐标缩放和透视插值形成 2.5D 视觉效果。核心判定必须以音频时钟为准，不得使用画面帧数、动画时间或逐帧累加的 `delta` 作为判定基准。

## 2. 术语

- **Rel key**：白色区域音符，根据音符覆盖的左、中、右区域判定。
- **Drm key**：红、绿、蓝颜色音符，只根据颜色通道判定，忽略音符的横向位置。
- **物量**：一个独立计分与判定对象。点击音符、长按头和每个长条物量点均分别计为一个物量。
- **SP**：Strictly Precise 奖励分；每个 Strictly Precise 判定增加 1 分。
- **视觉速度（SV）**：只控制音符在轨道上的视觉运动，不改变真实判定时刻。

## 3. 输入系统

### 3.1 逻辑通道与默认键位

| 类型 | 逻辑通道 | 默认物理键位 |
|---|---|---|
| Rel | 左区 | `A S D Z X C` |
| Rel | 中区 | `F G H J V B N` |
| Rel | 右区 | `K L ; M , .` |
| Drm | 红色 | `1 2 3 4 Q W E R` |
| Drm | 绿色 | `5 6 7 8 T Y U` |
| Drm | 蓝色 | `9 0 - = I O P [` |

输入必须按键盘的物理键位识别，而不是按输入字符识别，以避免输入法和键盘布局影响判定。

按键长按产生的系统重复事件不视为新的按下事件。

### 3.2 Rel key 判定

- 轨道分为左、中、右三个大区。
- Rel 音符可覆盖一个或多个大区。
- 按下音符实际覆盖的任意大区中的任意按键，即可作为合法输入。
- 音符大小只改变其覆盖范围，不代表需要按下更多按键。

### 3.3 Drm key 判定

- Drm 音符分为红、绿、蓝三种颜色。
- Drm 音符完全忽略横向位置和宽度，只接受对应颜色通道的按键。
- 横向位置和宽度只用于视觉编排。谱师可在低难度谱面中自行用位置构造输入提示，但引擎不赋予这种摆放额外规则。
- Drm 音符应使用独立外形、纹理或符号，不能只依赖颜色差异，以减少误读并兼顾色觉差异。

### 3.4 同时点击与输入匹配

- 同一逻辑通道可以同时出现两个或更多点击物量。
- 同时有几个点击物量，就需要几个独立的物理按键按下事件。
- 一个物理按键的单次按下事件最多匹配一个点击物量。
- 不要求具体键位，只要求按键属于该物量接受的逻辑通道。
- 匹配重叠的 Rel 接受范围时，应对同批输入和候选物量进行整体匹配，不能让处理顺序导致本可全部命中的音符漏判。

例如，同时存在接受“左或中”的物量 A 和接受“中或右”的物量 B 时，玩家按下中区键与右区键，应能分别匹配 A 和 B。

## 4. 音符类型

第一阶段包含两种音符形态：

- 点击音符
- 长按音符（无自动尾判）

两种形态都可以是 Rel key 或 Drm key。

### 4.1 点击音符

- 在判定窗口内检测一次新的按下事件。
- 输入成功后消耗该物量，不能用后续输入重新判定。
- 同时出现的多个点击物量分别计分、计准率和计连击。

### 4.2 长按头

- 长按头作为普通点击物量判定。
- 长按头计入总物量、分数、准率、SP 和连击。

### 4.3 长条物量点

- 长条身体不做持续不断线检测，只检查谱师显式放置的物量点。
- 到达物量点的准确时刻时，检查对应逻辑通道是否至少有一个合法按键处于按住状态。
- 命中物量点时直接判定为 **Strictly Precise**。
- 未按住时判定为 **Chaotic**，并重置连击。
- 漏掉某个物量点不会使整根长条失效；玩家可以在后续物量点重新接回。
- 两个物量点之间可以自由松开、换键或换区，不产生惩罚。
- 长按尾不会自动产生判定，但谱师可以在尾部时刻显式放置物量点。
- 没有身体物量点的长条在长按头判定后，身体仅作视觉表现；松开不会断连。

### 4.4 重叠长按

- 同一通道可以存在任意数量的重叠长按。
- 同一时刻多个同通道物量点可以共享一个合法的按住状态，并全部获得 Strictly Precise。
- 每个被命中的物量点仍分别增加分数、SP、准率和连击。
- 长按占用的按键不会自动触发后续点击音符；点击音符必须收到新的按下事件。

### 4.5 跨区 Rel 长条

- Rel 长条物量点根据该时刻长条实际覆盖的大区决定合法输入。
- 只要覆盖范围内任意一个大区存在按住的键，该物量点即可命中。
- 允许在覆盖范围内无条件换键、换区。

## 5. 横向轨道与音符几何

### 5.1 谱面网格

谱面编辑器和谱面数据始终使用 18 个等宽小格：

```text
左区：0～5
中区：6～11
右区：12～17
```

音符使用以下两个整数描述横向范围：

```text
StartLane：起始格，范围 0～17
LaneWidth：宽度，范围 1～18
```

覆盖范围采用左闭右开区间：

```text
[StartLane, StartLane + LaneWidth)
```

`StartLane + LaneWidth` 不得大于 18。

示例：

- `StartLane=5, LaneWidth=1`：只覆盖左区。
- `StartLane=5, LaneWidth=2`：覆盖第 5、6 格，横跨左区和中区。
- `StartLane=0, LaneWidth=18`：覆盖整个轨道。

### 5.2 游戏内辅助线

玩家可以选择 3、9、18 格三种轨道辅助线密度。该设置只改变轨道背景，不改变谱面、音符位置或判定。

| 显示设置 | 每个视觉格对应的谱面格 | 每个大区显示格数 |
|---|---:|---:|
| 3 格 | 6 | 1 |
| 9 格 | 2 | 3 |
| 18 格 | 1 | 6 |

谱面编辑器始终显示并吸附到 18 格。

## 6. 可移动、可变宽长条

- 长条由头、零个或多个控制点以及尾组成。
- 头、控制点和尾均使用整数 tick、整数起始格和整数宽度。
- 相邻控制点之间对起始位置和宽度进行线性插值。
- 编辑器中的控制点吸附到整数格；游戏渲染时允许使用小数坐标，以保证平滑运动。
- 长条可以左右移动、变宽、变窄并跨越不同大区。
- Rel 物量点在其时间位置上使用插值后的实际覆盖区域判定。
- Drm 长条的路径和宽度只影响视觉，判定仍只取决于颜色通道。
- 第一版只支持线性插值；缓入缓出和其他曲线插值留待后续考虑。

## 7. 时间系统

### 7.1 节拍精度

谱面使用整数 tick 保存时间位置，每个四分音符为：

```text
PPQN = 1920 ticks
```

示例：

```text
1 拍    = 1920 ticks
1/4 拍  = 480 ticks
1/8 拍  = 240 ticks
1/16 拍 = 120 ticks
三连音  = 640 ticks
```

编辑器提供常用节拍吸附，并允许直接输入 tick。

### 7.2 Timing 数据

每首歌曲共享以下 Timing 数据：

- 音频起始偏移量
- 初始 BPM
- 任意数量的 BPM 变化事件
- 拍号变化事件

BPM 负责把谱面 tick 转换为音频时间。拍号主要用于编辑器小节线和视觉显示。

### 7.3 判定时钟

- 所有判定均基于实际音频时钟。
- 判定不能依赖渲染帧率。
- 游戏必须支持全局音频与输入延迟校准。
- 音符的目标时间由 Timing 数据确定，不受视觉速度影响。

## 8. 独立视觉速度与特殊效果

视觉速度倍率的含义：

```text
 1.0：正常向判定线移动
 2.0：两倍视觉速度
 0.5：一半视觉速度
 0.0：视觉停止
负数：反向移动
```

- 支持独立视觉变速。
- 支持视觉停止。
- 支持视觉倒流。
- 玩家个人流速设置乘在谱面视觉速度之外。
- 停止和倒流只影响画面；音乐、谱面时间和判定时钟继续前进。
- 倒流可能使音符在目标时间前穿过判定线，但只有真实目标时间能够触发判定。

视觉位置通过累计视觉距离计算，不使用逐帧累加坐标：

```text
音符视觉距离 = 从当前节拍到目标节拍之间的视觉速度积分
```

该模型必须支持掉帧恢复、任意时间跳转、停止和倒流，并保证音符在自身判定时刻处于判定线上。

## 9. 判定系统

内部名称与显示名称完全一致。

| 判定 | 实际误差范围 | 基础分倍率 | 连击 | 基础准率份额 | 额外准率份额 | SP |
|---|---:|---:|---|---:|---:|---:|
| Strictly Precise | `|误差| ≤ 45 ms` | 100% | 保持并增加 | 100% | 1% | +1 |
| Precise | `45 < |误差| ≤ 75 ms` | 100% | 保持并增加 | 100% | 0 | 0 |
| Misaligned | `75 < |误差| ≤ 120 ms` | 50% | 保持并增加 | 50% | 0 | 0 |
| Chaotic | `120 < |误差| ≤ 165 ms` | 0% | 重置 | 0% | 0 | 0 |

补充规则：

- `-165 ms` 以前的输入不会影响该音符。
- 在 ±165 ms 内产生的合法点击输入会消耗对应音符，不能用后续输入重判。
- 超过目标时间 `+165 ms` 仍未判定的物量自动记为 Chaotic。
- 点击判定显示 `EARLY` 或 `LATE`。
- 长条物量点是基于按住状态的二元判定，不显示 EARLY/LATE；成功时直接为 Strictly Precise。
- 判定窗口应从配置读取，不在音符逻辑中硬编码。

## 10. 物量、分数与 SP

### 10.1 物量统计

```text
总物量 N = Rel 物量数 NR + Drm 物量数 ND
```

- 点击音符：1 物量。
- 长按头：1 物量。
- 每个长条物量点：1 物量。
- 长按尾：0 物量，除非谱师在该时刻显式放置物量点。
- 同时出现的多个音符分别计量。

总物量为 0 的谱面无效。

### 10.2 基础分池

- Rel key 满分：800,000。
- Drm key 满分：200,000。
- 基础总分满分：1,000,000。
- 两类物量分别在自己的分数池内平均分配。

将判定转换为整数单位：

```text
Strictly Precise = 2
Precise          = 2
Misaligned       = 1
Chaotic          = 0
```

设 `UR`、`UD` 分别为 Rel、Drm 已获得的判定单位总和：

```text
若 NR > 0：Rel 分 = floor(800000 × UR ÷ (2 × NR))
若 NR = 0：正常完成谱面时 Rel 分 = 800000

若 ND > 0：Drm 分 = floor(200000 × UD ÷ (2 × ND))
若 ND = 0：正常完成谱面时 Drm 分 = 200000

基础分 = Rel 分 + Drm 分
总分   = 基础分 + SP
```

缺少某类物量时获得的完整分数池：

- 只在正常完成谱面时结算。
- 不产生 SP。
- 不计入准率分母。
- 不产生连击。
- 中途退出时不结算。

### 10.3 SP

```text
SP = Strictly Precise 数量
SP 理论上限 = 总物量 N
理论最高总分 = 1,000,000 + N
```

长条物量点成功时直接获得 Strictly Precise，因此同样增加 1 SP。

## 11. 准率

分数与准率相互独立。准率满值为 101%，由基础 100% 和 Strictly Precise 额外 1% 组成。

设：

- `S`：Strictly Precise 数量
- `P`：Precise 数量
- `M`：Misaligned 数量
- `C`：Chaotic 数量
- `N = S + P + M + C`

计算公式：

```text
基础准率 = 100% × (S + P + 0.5M) ÷ N
额外准率 =   1% × S ÷ N
最终准率 = 基础准率 + 额外准率
```

典型结果：

| 全部判定 | 基础分 | SP | 准率 |
|---|---:|---:|---:|
| Strictly Precise | 1,000,000 | N | 101% |
| Precise | 1,000,000 | 0 | 100% |
| Misaligned | 500,000（两类均存在时） | 0 | 50% |
| Chaotic | 0（两类均存在时） | 0 | 0% |

## 12. 通关、连击与特殊完成标记

### 12.1 通关

- 暂不加入生命值。
- 暂不设置歌曲中途失败。
- 正常播放至歌曲结尾即为通关。
- 中途退出不算通关，也不触发缺失物量类型的分数补偿。

### 12.2 特殊完成标记

特殊完成标记全部使用大写，缩写取各单词首字母。

| 优先级 | 条件 | 显示名称 | 缩写 |
|---:|---|---|---|
| 1 | 全部为 Strictly Precise | `ALL STRICTLY PRECISE` | `ASP` |
| 2 | 只有 Strictly Precise 和 Precise | `ALL PRECISE` | `AP` |
| 3 | 没有 Chaotic，允许 Misaligned | `FULL COMBO` | `FC` |
| 4 | 存在 Chaotic | 无 | — |

逻辑条件：

```text
FC  = Chaotic 数量 == 0
AP  = FC && Misaligned 数量 == 0
ASP = AP && Precise 数量 == 0
```

结算界面只显示本局达到的最高优先级标记，但统计所有同时满足的低优先级标记：

| 本局最高成绩 | 结算显示 | 增加的次数统计 |
|---|---|---|
| ASP | `ALL STRICTLY PRECISE` | ASP、AP、FC 各 +1 |
| AP | `ALL PRECISE` | AP、FC 各 +1 |
| FC | `FULL COMBO` | FC +1 |
| 无标记 | 无 | 不增加 |

存档应保存：

- 每张谱面的历史最高标记。
- 每张谱面的 FC、AP、ASP 达成次数。
- 玩家全局 FC、AP、ASP 总次数。
- 全部谱面中至少达成过一次 FC、AP、ASP 的谱面数量。

## 13. 难度与谱面标识

难度数量和名称不在核心代码中硬编码。每张谱面保存：

- 稳定且唯一的谱面 ID。
- 难度显示名称。
- 难度排序值。
- 显示等级，例如 `12`、`14+`。
- 内部定数，例如 `12.4`、`14.8`。
- 谱师名称。
- 谱面格式版本。

玩家成绩必须通过稳定谱面 ID 关联，不能使用文件名作为记录键。

## 14. 谱面编辑器与歌曲包

### 14.1 编辑器原则

- 游戏本体和谱面编辑器位于同一个 Godot 项目。
- 编辑器使用与游戏相同的 Timing、视觉位置、音符和判定核心。
- 编辑器作为独立场景或独立导出模式运行。
- 编辑器横向网格固定为 18 格。
- 谱面保存为带格式版本的 JSON，并使用自定义扩展名 `.atendchart`。

### 14.2 歌曲包结构

建议目录结构：

```text
songs/
└─ song-id/
   ├─ song.json
   ├─ audio.ogg
   ├─ jacket.png
   ├─ timing.json
   └─ charts/
      ├─ chart-1.atendchart
      ├─ chart-2.atendchart
      └─ chart-3.atendchart
```

`song.json` 保存：

- 歌曲唯一 ID。
- 标题与艺术家。
- 音频和封面文件名。
- 预览开始时间与长度。
- 排序、分类等元数据。

`timing.json` 由同一首歌曲的所有难度共享，保存：

- 音频偏移。
- BPM 变化。
- 拍号变化。

每个 `.atendchart` 独立保存：

- 格式版本。
- 谱面唯一 ID。
- 难度元数据。
- 谱师。
- 独立视觉速度事件。
- 点击音符。
- 长条、路径控制点和物量点。

### 14.3 长条数据示例

```json
{
  "type": "hold",
  "inputType": "rel",
  "startTick": 19200,
  "endTick": 38400,
  "path": [
    { "tick": 19200, "lane": 1, "width": 4 },
    { "tick": 28800, "lane": 7, "width": 6 },
    { "tick": 38400, "lane": 13, "width": 3 }
  ],
  "judgeTicks": [21600, 24000, 28800, 33600]
}
```

Drm 音符额外保存 `red`、`green` 或 `blue`；其横向位置和宽度仍需保存，用于视觉表现。

## 15. 官方与玩家内容策略

第一版支持玩家导入、制作和分享歌曲包，但必须保留以后关闭玩家内容支持的能力。

### 15.1 内容模式

```text
OFFICIAL_ONLY
只加载官方歌曲，不显示导入、编辑器和玩家内容入口。

OFFICIAL_AND_LOCAL
加载官方歌曲和本地玩家歌曲，开放编辑器与导入功能。

EDITOR
用于制谱和测试，可作为独立导出版本。
```

第一版默认使用 `OFFICIAL_AND_LOCAL`。

工程应准备不同导出配置。关闭玩家内容时只改变内容策略和界面入口，不修改判定、谱面或播放核心，也不删除已有玩家谱面与成绩。

### 15.2 内容来源抽象

```text
SongSource
├─ OfficialSongSource
└─ LocalSongSource
```

官方内容和玩家内容使用相同的数据结构，但从不同来源加载。

成绩记录使用组合标识：

```text
内容来源 + 歌曲包 ID + 谱面 ID
```

官方命名空间保留，玩家歌曲包不能使用，以防玩家内容冒充官方内容或覆盖官方成绩。

### 15.3 外部内容安全要求

- 外部歌曲包只允许数据、图片和音频，不允许携带脚本或 Godot 场景。
- 校验 JSON 结构、枚举值和格式版本。
- 限制文件大小、音频长度和图片尺寸。
- 阻止路径穿越，例如拒绝 `../` 和包目录以外的路径。
- 重复 ID 必须提示冲突，不得静默覆盖。
- 单个歌曲包加载失败时只跳过该包，不得影响整个游戏启动。
- 第一版使用普通文件夹，后续再考虑压缩包导入和导出。

## 16. 已延期或尚未确定的事项

以下内容不属于当前设计基线，后续另行讨论：

- 生命值系统。
- 歌曲中途失败条件。
- 结算等级或 Rank 阈值。
- 非线性长条插值。
- 压缩歌曲包及正式分享流程。
- 玩家内容是否在未来发行版本中保持启用。
- 视觉风格、UI 布局、特效和音符美术。
- 键位重映射与键盘冲突/NKRO 检测界面。
- 回放、排行榜和反作弊。

## 17. 下一步建议

进入原型阶段前，优先完成以下工作：

1. 创建 Godot 4 + C# 工程与基础目录结构。
2. 建立纯 C# 的 Timing、BPM 映射、判定窗口和计分模型，并编写自动化测试。
3. 实现物理键位输入采集与六个逻辑通道。
4. 实现 18 格斜轨道及单个 Rel/Drm 点击音符。
5. 接入音频时钟和延迟校准。
6. 实现同时物量的整体输入匹配。
7. 实现长条路径、物量点和重叠长按。
8. 实现视觉速度积分、停止和倒流。
9. 最后实现最小谱面编辑器与歌曲包加载。

原型的首个验收目标是：使用一首测试音频，从 JSON 谱面加载 Rel/Drm 点击音符，在音频时钟驱动下完成输入、判定、分数、准率、SP、连击和结算。
